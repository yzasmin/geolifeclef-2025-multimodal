data:
  num_workers: 2

training:
  batch_size: 64
  lr: 3e-4
  epochs: 20
  patience: 4
  max_train_samples: 1200000
  max_val_samples: 100000

pseudo_label:
  t_pos: 0.5
  t_neg: 0.03
  max_negatives_per_sample: 64

output:
  log_interval: 100
